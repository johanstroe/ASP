@model IEnumerable<Projects>

@{
    ViewData["Title"] = "Projects view";
}


<partial name="Partials/Sections/_AddProjectForm" model="new AddProjectForm()" />



<section id="projects">
    <header>
        <h2>Projects</h2>
        <button type="button" data-modal="true" data-target="#addProjectModal" class="btn btn-add">Projects</button>
    </header>
    <div class="surface surface-cards">


        @if (Model != null)
        {
            foreach(var project in Model)
            {
                <partial name="Partials/Sections/_ProjectList" model="Model" />
            }
        }
 
    </div>
</section>


